---
description: Show the recommended next ticket to work on
---
Run `node ${CLAUDE_PLUGIN_ROOT}/dist/server.js next` (or `ticketgraph next` if globally installed) and report the recommended next ticket and the reason (priority, age, no open blockers). Equivalent to `tickets.next` when the MCP server is enabled.
