---
description: Show ticket counts and point totals for the current project
---
Run `node ${CLAUDE_PLUGIN_ROOT}/dist/server.js stats` (or `ticketgraph stats` if globally installed) and summarise the counts (by status, priority, epic, and type) and point totals. Equivalent to `tickets.stats` when the MCP server is enabled.
