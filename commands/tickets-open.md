---
description: List all open, in-progress, and blocked tickets
---
Use the `tickets.list` MCP tool (default status filter covers open, in_progress, and blocked) and show the outstanding tickets as a compact list (id, title, status, priority).
